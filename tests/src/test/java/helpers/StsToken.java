package helpers;

import io.restassured.path.json.JsonPath;

import java.util.Properties;

public class StsToken {

    public  StsToken(){};

    public String GeraToken(PropertiesUtils props){
        JsonPath resp = null;
        if(props.getUseProxy()) {String proxyUrl = "url";
        };
        return "";
    }
}
